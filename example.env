# Copy to .env (never commit .env) and fill values
# Permissions: chmod 600 .env
# Copernicus Data Space Ecosystem authentication uses a public client id (cdse-public)
# with the Resource Owner Password Credentials (grant_type=password) or a refresh token.
# Provide EITHER username/password OR a refresh token (preferred after first acquisition).

COPERNICUS_USERNAME=your_username_here
COPERNICUS_PASSWORD=your_password_here

# Optional: once you obtain a refresh token you can store it instead of password
COPERNICUS_REFRESH_TOKEN=

# Optional override (defaults to cdse-public if unset)
COPERNICUS_CLIENT_ID=cdse-public
